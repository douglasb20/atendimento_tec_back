import * as path from 'path';

export const FILES_PATH = path.resolve(process.cwd(), 'files');
export const IMAGES_PATH = path.resolve(FILES_PATH, 'images');
export const AVATAR_PATH = path.resolve(IMAGES_PATH, 'avatar');
